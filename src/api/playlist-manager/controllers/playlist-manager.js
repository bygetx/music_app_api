'use strict';

/**
 * playlist-manager controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::playlist-manager.playlist-manager');
